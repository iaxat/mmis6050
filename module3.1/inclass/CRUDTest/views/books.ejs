<%- include("_header") -%>

<h1>List of Books (<%= model.length %>)</h1>

<div class="table-responsive-sm">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Title</th>
        <th>Author</th>
        <th>Comment</th>
        <th class="d-print-none">
          <a class="btn btn-sm btn-success" href="/create">Add new</a>
        </th>
      </tr>
    </thead>
    <tbody>
      <% for (const book of model) { %>
        <tr>
          <td><%= book.Title %></td>
          <td><%= book.Author %></td>
          <td><%= book.Comment %></td>
          <td class="d-print-none">
            <a class="btn btn-sm btn-warning" href="/edit/<%= book._id %>">Edit</a>
            <a class="btn btn-sm btn-danger" href="/delete/<%= book._id %>">Delete</a>
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>


<%- include("_footer") -%>
